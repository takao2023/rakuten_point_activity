<nav class="navbar navbar-expand-lg glass-header sticky-top">
  <div class="container-fluid px-3">
    <div class="d-flex align-items-center">
      <%= link_to (user_signed_in? ? dashboards_path : root_path), class: "navbar-brand d-flex align-items-center" do %>
        <div class="logo-p me-3">
          <span>P</span>
        </div>
        <span class="text-white fw-bold fs-4">楽ポイ活マネジメント</span>
      <% end %>
    </div>

    <% if user_signed_in? %>
      <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain" aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
        <i class="bi bi-list text-white fs-1"></i>
      </button>

      <div class="collapse navbar-collapse" id="navbarMain">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0 pt-2 pb-3 pt-lg-0 pb-lg-0 align-items-lg-center">
          <li class="nav-item">
            <%= link_to courses_path, class: "nav-link text-white py-2 px-3", data: { turbo: false } do %>
              <i class="bi bi-compass me-2"></i>コース選択
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to report_point_activity_targets_path, class: "nav-link text-white py-2 px-3", data: { turbo: false } do %>
              <i class="bi bi-check2-square me-2"></i>完了報告
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to point_activity_targets_path, class: "nav-link text-white py-2 px-3", data: { turbo: false } do %>
              <i class="bi bi-bar-chart-line me-2"></i>進捗確認
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to achievements_path, class: "nav-link text-white py-2 px-3", data: { turbo: false } do %>
              <i class="bi bi-award me-2"></i>実績コレクション
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to campaigns_path, class: "nav-link text-white py-2 px-3", data: { turbo: false } do %>
              <i class="bi bi-calendar3 me-2"></i>カレンダー
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to edit_user_registration_path, class: "nav-link text-white py-2 px-3", data: { turbo: false } do %>
              <i class="bi bi-person-circle me-2"></i>マイページ
            <% end %>
          </li>
          <li class="nav-item ms-lg-3">
            <%= button_to destroy_user_session_path, method: :delete, class: "btn btn-outline-light btn-sm w-100 rounded-pill px-4", form: { data: { turbo: false } } do %>
              <i class="bi bi-box-arrow-right me-2"></i>ログアウト
            <% end %>
          </li>
        </ul>
      </div>
    <% else %>
      <div class="d-flex gap-2">
        <%= link_to 'ログイン', new_user_session_path, class: "btn btn-sm btn-outline-light rounded-pill px-3" %>
      </div>
    <% end %>
  </div>
</nav>


